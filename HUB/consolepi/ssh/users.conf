# Optional extra SSH users for the ConsolePi container.
# Format: username:mode:password_hash
# mode: menu or shell
# Backward compatible format username:password_hash is treated as shell mode.
# Example:
# opsadmin:menu:$6$rounds=656000$abc...$xyz...
# nocadmin:shell:$6$rounds=656000$def...$uvw...